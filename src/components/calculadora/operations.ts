export const handleSwitchNumbers = (text: string) => {
  switch (text) {
    case "1":
      return parseInt(text);

    case "2":
      return parseInt(text);

    case "3":
      return parseInt(text);

    case "4":
      return parseInt(text);

    case "5":
      return parseInt(text);

    case "6":
      return parseInt(text);

    case "7":
      return parseInt(text);

    case "8":
      return parseInt(text);

    case "9":
      return parseInt(text);

    default:
  }
};
